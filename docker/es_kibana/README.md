## Elasticsearch and Kibana Using `docker-compose`

   1. `sudo docker ps  # make sure docker is running and elasticsearch is not`
   1. `sudo sysctl -w vm.max_map_count=262144`
   1. `sudo docker-compose up`

