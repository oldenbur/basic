[project]
name = "interview"
version = "0.1.0"
description = "Practice scripts for coding interviews"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dataclasses>=0.8",
    "pymemcache>=4.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
