[project]
name = "interview"
version = "0.1.0"
description = "Practice scripts for coding interviews"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dataclasses>=0.8",
    "pytest>=9.0.3",
]
