[project]
name = "spark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Paul Oldenburg", email = "oldenbur@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.2.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
