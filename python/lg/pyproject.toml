[project]
name = "lg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.2.7",
    "langgraph>=1.0.7",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
